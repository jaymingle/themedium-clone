import {Box} from "@mui/material";

const Rightbar = () => {
    return (
        <Box flex={2} sx={{background: 'green', display:{sx: 'none'}}}>
            The Rightbar
        </Box>
    );
};

export default Rightbar;