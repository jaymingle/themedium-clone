import {Box} from "@mui/material";

const Posts = () => {
    return (
        <Box flex={7} sx={{background: 'brown', marginLeft:{xs: 0, sm:5}}}>
            The Post
        </Box>
    );
};

export default Posts;