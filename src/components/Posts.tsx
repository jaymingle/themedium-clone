import {Box} from "@mui/material";

const Posts = () => {
    return (
        <Box flex={7} sx={{background: 'brown', display:{xs: 'none', sm:'block'}}}>
            The Post
        </Box>
    );
};

export default Posts;