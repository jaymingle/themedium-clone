import {Box} from "@mui/material";

const Posts = () => {
    return (
        <Box flex={7} sx={{background: 'brown', paddingX:{xs: 0, sm: 0, lg: 20}}}>
            The Post
        </Box>
    );
};

export default Posts;