import {Box} from "@mui/material";

const Posts = () => {
    return (
        <Box flex={6} sx={{background: 'brown', display:{xs: 'block'}}}>
            The Post
        </Box>
    );
};

export default Posts;