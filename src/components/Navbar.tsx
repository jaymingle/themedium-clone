import {AppBar, Toolbar} from "@mui/material";

const Navbar = () => {
    return (
        <AppBar sx={{}} position="static">
            <Toolbar>
                Navbar
            </Toolbar>
        </AppBar>
    );
};

export default Navbar;