import {AppBar, Toolbar} from "@mui/material";

const Navbar = () => {
    return (
        <AppBar sx={{}}>
            <Toolbar position="static">
                Navbar
            </Toolbar>
        </AppBar>
    );
};

export default Navbar;